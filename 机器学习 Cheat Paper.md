# 模型选择和评估
## 模型评估和损失函数
### 一、 混淆矩阵基础
混淆矩阵用于二分类问题，根据**真实类别**与**预测类别**的组合分为四种情况。记忆口诀：**前一个字母表示“预测对没对”（True/False），后一个字母表示“预测出的类别”（Positive/Negative）。**
1.  **TP**：预测为正，实际也为正（真阳性）。
2.  **TN**：预测为负，实际也为负（真阴性）。
3.  **FP**：预测为正，但实际为负（假阳性，**误报**）。
4.  **FN**：预测为负，但实际为正（假阴性，**漏报**）。
### 二、 核心性能指标
考试重点是记住**分母是谁**。
#### 1. 准确率 Accuracy
*   **公式**：$\frac{TP + TN}{总样本数}$
*   **含义**：所有样本中预测对的比例。
*   **缺点**：当样本**类别不平衡**时（例如正样本占99%），该指标会失效（全猜正也有99%准确率），此时不能只看 Accuracy。
#### 2. 查准率 Precision
*   **公式**：$\frac{TP}{TP + FP}$
*   **分母**：所有**被预测为正**的样本。
*   **含义**：我预测是正例的样本里，有多少是真的正例？
*   **场景**：**宁缺毋滥**。例如垃圾邮件过滤（主要怕把正常邮件误判为垃圾，即怕 FP）。
#### 3. 查全率 Recall
*   **公式**：$\frac{TP}{TP + FN}$
*   **分母**：所有**真实为正**的样本。
*   **含义**：所有真的正例里，我找回了多少？
*   **场景**：**宁可错杀不可放过**。例如癌症筛查、地震预测（主要怕漏掉病人，即怕 FN）。
#### 4. F1 分数
*   **公式**：$\frac{2 \times P \times R}{P + R}$
*   **含义**：查准率和查全率的**调和平均数**。
*   **特点**：只有当 P 和 R 都高时，F1 才高。用于综合评估模型性能，平衡了 Precision 和 Recall 的矛盾。
### 三、 ROC 曲线与 AUC
*   **横轴**：FPR（假正例率）$= \frac{FP}{TN+FP}$。代表负样本中被误判的比例（代价）。
*   **纵轴**：TPR（真正例率，即 Recall）。代表正样本中被找出的比例（收益）。
*   **形状**：曲线越靠近**左上角**越好。
*   **AUC**：ROC 曲线下的面积。
    *   AUC = 0.5：随机猜测。
    *   AUC = 1.0：完美分类。
    *   **特点**：AUC 对样本类别**不平衡不敏感**，是评估排序能力的金标准。
### 四、 常见损失函数 Loss
损失函数衡量模型预测值与真实值之间的差距，越小越好。
#### 1. 回归问题（预测数值）
*   **均方误差 MSE (L2 Loss)**
    *   **公式**：$\frac{1}{N}\sum (y - \hat{y})^2$
    *   **特点**：处处可导，计算方便；但因为有平方，对**异常值（离群点）非常敏感**，异常值会拉大 Loss。
*   **平均绝对误差 MAE (L1 Loss)**
    *   **公式**：$\frac{1}{N}\sum |y - \hat{y}|$
    *   **特点**：对异常值**鲁棒**（不敏感）；但在 0 处不可导，梯度计算稍麻烦。
#### 2. 分类问题（预测类别）
*   **交叉熵损失 Cross-Entropy**
    *   **应用**：逻辑回归、神经网络（Softmax）。
    *   **公式**（二分类）：$-\sum [y \ln \hat{y} + (1-y) \ln (1-\hat{y})]$
    *   **特点**：衡量两个概率分布的差异。预测越准，Loss 越接近 0；如果对错误类别非常自信，Loss 会爆增（惩罚力度大）。
*   **Hinge Loss**
    *   **应用**：支持向量机 SVM。
    *   **公式**：$\max(0, 1 - y \cdot \hat{y})$ （假设标签 $y$ 为 +1/-1）
    *   **特点**：只要分类正确且距离分界线足够远（间隔大于1），Loss 就为 0。不关注概率，只关注是否被“撑开”了间隔。
## KNN算法
### 一、 KNN 算法流程（四步走）
KNN 是一种**懒惰学习**算法（没有显式的训练过程），预测时才进行计算。
1.  **算距离**：
    计算待分类样本与训练集中**所有**样本点之间的距离（常用欧氏距离）。
2.  **找邻居**：
    按照距离从小到大排序，选取距离最近的 **K** 个样本作为邻居。
3.  **做决策**：
    *   **分类任务**：采用**多数表决法**（投票）。K 个邻居中哪个类别出现次数最多，就预测为该类别。
    *   **回归任务**：采用**平均法**。计算 K 个邻居标签值的平均值作为预测结果。
4.  **输出结果**：得到最终的预测分类或数值。
### 二、 K 值选择对模型的影响（核心考点）
K 值的选择本质上是在**偏差**与**方差**之间做权衡。
#### 1. 选择较小的 K 值（例如 K=1）
*   **决策边界**：**崎岖、支离破碎**。模型会刻意去包围每一个训练点。
*   **模型复杂度**：**高**。模型试图拟合数据的每一个细节。
*   **偏差**：**低**。模型对训练数据的拟合程度很高，训练误差几乎为 0。
*   **方差**：**高**。模型对数据中的噪声和微小变化极其敏感，稍微换一组数据，结果变化很大。
*   **结果倾向**：容易导致**过拟合**。
#### 2. 选择较大的 K 值（例如 K=N）
*   **决策边界**：**平滑、简单**。局部细节被抹平，趋向于整体分布。
*   **模型复杂度**：**低**。模型忽略局部特征，只看整体趋势。
*   **偏差**：**高**。模型学不到局部的规律，训练误差变大。
*   **方差**：**低**。模型非常稳定，个别噪声点无法影响整体投票结果。
*   **结果倾向**：容易导致**欠拟合**。
#### 3. 考试满分逻辑
*   **K 值越小** $\rightarrow$ 模型越复杂 $\rightarrow$ 偏差小、方差大 $\rightarrow$ **过拟合**。
*   **K 值越大** $\rightarrow$ 模型越简单 $\rightarrow$ 偏差大、方差小 $\rightarrow$ **欠拟合**。
*   **实际操作**：通常通过**交叉验证**来选择最优的 K 值。

# 线性模型
## 逻辑回归
### **考点一：为什么优化函数使用条件分布 $P(Y|x)$？**
**核心答案：逻辑回归是判别式模型**
1.  **任务目标直接**：LR 的目标是**分类**（寻找决策边界），即已知特征 $x$ 预测类别 $Y$。直接对条件概率 $P(Y|x)$ 建模最直接、最高效。
2.  **避开复杂假设**：联合分布 $P(x,Y) = P(Y|x)P(x)$ 需要对特征分布 $P(x)$ 进行建模。在高维空间中，特征分布通常极其复杂且未知。
3.  **鲁棒性强**：因为不需要假设特征 $x$ 服从特定分布（如高斯分布），LR 对各类特征（连续/离散）的适应性更强，泛化能力更好。
### **考点二：逻辑回归的标准推导（四步走）**
**第一步：定义模型（Sigmoid 映射）**
将线性回归的结果 $z = w^Tx$ 映射到 $(0,1)$ 区间，表示概率：
$$ h_w(x) = P(y=1|x) = \frac{1}{1 + e^{-w^T x}} $$
**第二步：整合概率表达式**
对于二分类 $y \in \{0, 1\}$，将正负样本的概率合并为一个公式：
$$ P(y|x; w) = [h_w(x)]^y \cdot [1 - h_w(x)]^{1-y} $$
*（注：当 $y=1$ 时保留前半部分，当 $y=0$ 时保留后半部分）*
**第三步：构造极大似然函数 (MLE)**
假设 $N$ 个样本相互独立，联合概率（似然函数）为所有样本概率的乘积：
$$ L(w) = \prod_{i=1}^{N} P(y_i|x_i; w) = \prod_{i=1}^{N} [h_w(x_i)]^{y_i} [1 - h_w(x_i)]^{1-y_i} $$
**第四步：取对数求最小损失**
为了方便计算（变乘为加）并防止下溢，取**对数似然**；为了符合“最小化损失”的习惯，**取负号**：
$$ J(w) = -\ln L(w) = -\sum_{i=1}^{N} \left[ y_i \ln h_w(x_i) + (1-y_i) \ln(1 - h_w(x_i)) \right] $$
这就是著名的**交叉熵损失函数**。
> **补充（如果问怎么解）**：由于 $J(w)$ 没有闭式解（解析解），通常使用**梯度下降法**迭代求解：$w \leftarrow w - \eta \nabla J(w)$。

## 感知器算法
### 一、 感知器参数更新公式的推导
考试如果问：“感知器的权重更新公式 $W \leftarrow W + \eta X$ 是怎么来的？”，请按以下逻辑回答：
感知器的学习本质是**随机梯度下降法**在特定损失函数上的应用。
1.  **定义损失函数**：
    感知器不使用均方误差，而是使用**误分类点到超平面的距离总和**。
    假设误分类点的集合为 $M$。对于误分类点 $(x_i, y_i)$，因为分错了，所以 $-y_i(w^T x_i + b) > 0$（即距离为正数）。
    损失函数定义为所有误分类点的函数间隔之和：$$ L(w, b) = - \sum_{x_i \in M} y_i (w^T x_i + b) $$
2.  **求梯度**：
    对权重 $w$ 求偏导（梯度）：$$ \nabla_w L = - \sum_{x_i \in M} y_i x_i $$
3.  **随机梯度下降**：
    我们希望最小化损失函数，所以沿着梯度的**反方向**更新权重。
    假设我们每次只随机选取**一个**误分类点 $(x_i, y_i)$ 进行更新。
    更新公式为：$$ w \leftarrow w - \eta \cdot \nabla_w L $$
    代入梯度的表达式（$-y_i x_i$）：$$ w \leftarrow w - \eta \cdot (-y_i x_i) ,即 w \leftarrow w + \eta y_i x_i $$
    **结论**：这就是为什么当一个正样本分错时（$y=+1$），权重加上 $x$；当一个负样本分错时（$y=-1$），权重减去 $x$。
### 二、 手算解题标准流程（三步走）
做计算题时，为了统一符号，防止正负号搞混，建议使用**规范化**的方法。
**第一步：数据预处理（必做）**
1.  **增广化**：给所有样本向量末尾加一个 **1**（把截距 $b$ 并入 $W$）。
2.  **规范化**：把所有**第二类（负类）** 的样本向量**乘以 -1**。
    *   目的：处理后，无论哪一类，只要 $W^T Y > 0$ 就是对的，$\le 0$ 就是错的。
**第二步：迭代计算**
按顺序遍历样本，计算判别值 $val = W^T Y$：
*   若 $val > 0$：分类正确，**跳过**。
*   若 $val \le 0$：分类错误，**更新权重**。
    *   **公式**：$W_{new} = W_{old} + Y$ （假设学习率 $\eta=1$）
**第三步：收敛判断**
直到完整的一轮遍历中，**没有发生任何权重更新**，算法结束。
### 三、 真题实战演练
**题目**：
两类样本：
$\omega_1$: $X_1=[0,0]^T, X_2=[0,1]^T$
$\omega_2$: $X_3=[1,0]^T, X_4=[1,1]^T$
初始权重 $W(1) = [0, 1, 1]^T$（已增广）。求解判别函数。
#### 1. 数据预处理
*   **$\omega_1$ (正类，补1)**：
    $Y_1 = [0, 0, 1]^T$
    $Y_2 = [0, 1, 1]^T$
*   **$\omega_2$ (负类，补1再取反)**：
    $Y_3 = [-1, 0, -1]^T$
    $Y_4 = [-1, -1, -1]^T$
#### 2. 迭代过程
**第一轮迭代：**
1.  **查 $Y_1$**：$W(1)^T Y_1 = 0(0)+1(0)+1(1) = 1 > 0$。**对**。
2.  **查 $Y_2$**：$W(1)^T Y_2 = 0(0)+1(1)+1(1) = 2 > 0$。**对**。
3.  **查 $Y_3$**：$W(1)^T Y_3 = 0(-1)+1(0)+1(-1) = -1 \le 0$。**错！**
    *   **更新**：$W(2) = W(1) + Y_3 = [0, 1, 1] + [-1, 0, -1] = [-1, 1, 0]^T$
4.  **查 $Y_4$** (用新W)：$W(2)^T Y_4 = -1(-1)+1(-1)+0(-1) = 0 \le 0$。**错！**
    *   **更新**：$W(3) = W(2) + Y_4 = [-1, 1, 0] + [-1, -1, -1] = [-2, 0, -1]^T$
**第二轮迭代（起点 $W=[-2, 0, -1]^T$）：**
1.  **查 $Y_1$**：$W(3)^T Y_1 = -2(0)+0(0)-1(1) = -1 \le 0$。**错！**
    *   **更新**：$W(4) = W(3) + Y_1 = [-2, 0, -1] + [0, 0, 1] = [-2, 0, 0]^T$
2.  **查 $Y_2$**：$W(4)^T Y_2 = -2(0)+0(1)+0(1) = 0 \le 0$。**错！**
    *   **更新**：$W(5) = W(4) + Y_2 = [-2, 0, 0] + [0, 1, 1] = [-2, 1, 1]^T$
3.  **查 $Y_3$**：$W(5)^T Y_3 = -2(-1)+1(0)+1(-1) = 1 > 0$。**对**。
4.  **查 $Y_4$**：$W(5)^T Y_4 = -2(-1)+1(-1)+1(-1) = 0 \le 0$。**错！**
    *   **更新**：$W(6) = W(5) + Y_4 = [-2, 1, 1] + [-1, -1, -1] = [-3, 0, 0]^T$
**第三轮迭代（起点 $W=[-3, 0, 0]^T$）：**
1.  **查 $Y_1$**：$W(6)^T Y_1 = -3(0)+0(0)+0(1) = 0 \le 0$。**错！**
    *   **更新**：$W(7) = W(6) + Y_1 = [-3, 0, 0] + [0, 0, 1] = [-3, 0, 1]^T$
2.  **查 $Y_2$**：$W(7)^T Y_2 = 1 > 0$。**对**。
3.  **查 $Y_3$**：$W(7)^T Y_3 = 2 > 0$。**对**。
4.  **查 $Y_4$**：$W(7)^T Y_4 = 2 > 0$。**对**。
**第四轮验证：**
回头查 $Y_1$，结果为 $1 > 0$，正确。
**所有样本全部正确，收敛。**
#### 3. 最终结果
*   **解向量**：$W = [-3, 0, 1]^T$
*   **判别函数**：$g(x) = -3x_1 + 0x_2 + 1 = -3x_1 + 1$
*   **分界线方程**：$-3x_1 + 1 = 0 \Rightarrow x_1 = 1/3$
# 贝叶斯决策
## 先验、似然、后验、极大似然、最大后验，反正是核心概念
### 一、 核心概念速查（必背）
在贝叶斯公式 $P(\omega|x) = \frac{P(x|\omega)P(\omega)}{P(x)}$ 中：
1.  **先验概率** $P(\omega)$：
    *   **含义**：在没看到具体数据 $x$ 之前，根据历史经验得出的类别发生的概率。
    *   **例子**：感冒比埃博拉病毒常见，$P(感冒) > P(埃博拉)$。
    *   **题目特征**：直接给出的概率值，如“正常人的概率是0.99”。
2.  **似然函数** $P(x|\omega)$：
    *   **含义**：已知属于某一类 $\omega$ 的前提下，长成数据 $x$ 这样子的概率。
    *   **例子**：得感冒的人里，发烧的概率是多少。
    *   **题目特征**：通常给出高斯分布公式，或者具体的概率表达式（如硬币题中的 $\theta$ 和 $2\theta$）。
3.  **后验概率** $P(\omega|x)$：
    *   **含义**：看到数据 $x$ 之后，推断它属于类别 $\omega$ 的概率。
    *   **计算**：**后验 $\propto$ 似然 $\times$ 先验**。分母 $P(x)$ 对于所有类都一样，比较大小时可忽略。
4.  **对数似然** $\ln L(\theta)$：
    *   **含义**：给似然函数取自然对数。
    *   **作用**：
        1.  把连乘 $\prod$ 变成累加 $\sum$，方便求导。
        2.  消去指数函数中的 $e$，简化计算。
        3.  单调递增，不改变极值点的位置。
5.  **0-1 损失函数**：
    *   **含义**：猜对罚0分，猜错罚1分。
    *   **决策规则**：等价于**最小错误率决策**，即**谁的后验概率大就选谁**。
### 二、 极大似然估计（MLE）
**核心思想**：
既然事情（数据 $D$）已经发生了，那么参数 $\theta$ 应该是让这件事**发生的概率最大**的那个值。
即：求 $\theta$ 使得 $P(D|\theta)$ 最大。
**标准解题四步走（万能模板）：**
1.  **写出似然函数**：$L(\theta) = \prod P(x_i|\theta)$ （所有样本概率乘起来）。
2.  **取对数**：$\ln L(\theta)$ （乘法变加法，指数落下）。
3.  **求导数**：$\frac{\partial \ln L}{\partial \theta} = 0$。
4.  **解方程**：解出的 $\theta$ 即为估计值。
#### **【例题 1：抛硬币求参数】**
**题目**：硬币1正面概率 $\theta$，硬币2正面概率 $2\theta$。抛五次结果：(硬币1,正)、(硬币2,反)、(硬币2,反)、(硬币2,反)、(硬币2,正)。求 $\theta$。
**解答**：
1.  **写似然函数**：$$L(\theta) = (\theta) \times (1-2\theta) \times (1-2\theta) \times (1-2\theta) \times (2\theta)$$整理得：$L(\theta) = 2\theta^2 (1-2\theta)^3$
2.  **取对数**：$$\ln L(\theta) = \ln 2 + 2\ln \theta + 3\ln(1-2\theta)$$
3.  **求导并令为0**：$$\frac{d}{d\theta} = \frac{2}{\theta} + 3 \cdot \frac{1}{1-2\theta} \cdot (-2) = \frac{2}{\theta} - \frac{6}{1-2\theta} = 0$$
4.  **求解**：$$\frac{2}{\theta} = \frac{6}{1-2\theta} \implies 2(1-2\theta) = 6\theta \implies 2 = 10\theta \implies \theta = 0.2$$
### 三、 贝叶斯决策（最小风险/错误率）
**核心思想**：
结合**先验概率**和**似然概率**，计算后验概率进行比较。
**决策规则**：
比较 **类别1的得分** vs **类别2的得分**。$$ \text{得分}_i = P(x|\omega_i) \cdot P(\omega_i) $$也就是：**似然 $\times$ 先验**。
#### **【例题 2：高斯分布分类】**
**题目条件**：
*   先验：$P(\omega_1) = e \cdot P(\omega_2)$。
*   似然：两类均为正态分布，均值 $\mu_1=0, \mu_2=2$，方差 $\sigma^2=4$。
*   损失：0-1损失（即比较后验概率大小）。
*   求：分类阈值 $x$。
**解答**：
1.  **列出不等式**（判为 $\omega_1$ 的条件）：$$ P(x|\omega_1)P(\omega_1) > P(x|\omega_2)P(\omega_2) $$
2.  **取对数化简**（高斯分布必做步骤）：$$ \ln P(x|\omega_1) + \ln P(\omega_1) > \ln P(x|\omega_2) + \ln P(\omega_2) $$
3.  **代入公式**：
    *   $\ln P(\omega_1) - \ln P(\omega_2) = \ln e = 1$
    *   $\ln P(x|\omega) = -\frac{(x-\mu)^2}{2\sigma^2} + \text{常数}$
    *   不等式变为：$$ -\frac{(x-0)^2}{8} + 1 > -\frac{(x-2)^2}{8} $$
4.  **解不等式**：$$ -(x^2) + 8 > -(x^2 - 4x + 4) $$$$ -x^2 + 8 > -x^2 + 4x - 4 $$$$ 12 > 4x \implies x < 3 $$
5. **结论**：阈值为 3。当 $x < 3$ 时判为 $\omega_1$，否则判为 $\omega_2$。
### 极大似然估计 (MLE) 和最大后验估计 (MAP) 的区别及过拟合解释
**（1）MLE 和 MAP 的区别：**
*   **核心思想不同**：
    *   **MLE (Maximum Likelihood Estimation)**：认为参数 $\theta$ 是一个未知的**固定常数**。它的目标是找到一组参数，使得这组参数产生当前观测数据的概率（似然度 $P(X|\theta)$）最大。
    *   **MAP (Maximum A Posteriori)**：认为参数 $\theta$ 是一个**随机变量**，服从某种分布。它的目标是找到一组参数，使得在观测到当前数据的前提下，参数出现的概率（后验概率 $P(\theta|X)$）最大。
*   **计算公式不同**：
    *   $\hat{\theta}_{MLE} = \arg\max P(X|\theta)$
    *   $\hat{\theta}_{MAP} = \arg\max P(\theta|X) = \arg\max P(X|\theta) \cdot P(\theta)$
    *   **结论**：**MAP = MLE + 先验概率 (Prior)**。如果假设先验概率 $P(\theta)$ 是均匀分布（常数），那么 MAP 就退化为 MLE。
**（2）为什么 MLE 比 MAP 更容易过拟合？**
*   **MLE 唯数据论**：MLE 完全依赖于当前的训练数据。如果数据量少、或者数据中存在噪声（Noise），MLE 会试图强行拟合这些噪声，导致计算出的参数偏离真实值（例如：抛硬币3次全是正面，MLE 会认为正面概率是 100%）。
*   **MAP 引入了正则化**：MAP 引入了**先验概率 $P(\theta)$**，这相当于给参数加了一个“约束”或“惩罚项”。即使数据呈现出某种极端的分布，先验知识会把参数往“正常值”拉回来。
    *   *数学本质*：在机器学习中，引入先验分布（如高斯分布）等价于在 Loss 函数中加入 **L2 正则化**。正则化的作用正是为了抑制模型复杂度，防止过拟合。因此，没有先验约束的 MLE 更容易过拟合。
## 贝叶斯决策(最小错误率、最小风险)，朴素贝叶斯
### 一、 贝叶斯决策论（理论基础）
**核心思想**：利用**先验概率**和**类条件概率（似然）**，算出**后验概率**，然后根据后验概率做决策。
#### 1. 最小错误率贝叶斯决策
*   **目标**：让犯错的可能性最小（准确率最高）。
*   **决策规则**：**谁的后验概率大选谁**。
    *   比较 $P(\omega_1|x)$ 和 $P(\omega_2|x)$。
    *   根据贝叶斯公式 $P(\omega|x) = \frac{P(x|\omega)P(\omega)}{P(x)}$，分母 $P(x)$ 对所有类相同。
    *   **实际操作**：直接比较 $P(x|\omega_1)P(\omega_1)$ 与 $P(x|\omega_2)P(\omega_2)$ 的大小。
#### 2. 最小风险贝叶斯决策
*   **目标**：让平均损失（风险）最小。适用于不同错误代价不同的情况（如把癌症误判为正常的代价远大于把正常误判为癌症）。
*   **引入**：损失矩阵 $\lambda_{ij}$（把真实为 $j$ 类误判为 $i$ 类的损失）。
*   **计算条件风险**：
    *   判为 1 类的风险：$R(\alpha_1|x) = \lambda_{11}P(\omega_1|x) + \lambda_{12}P(\omega_2|x)$
    *   判为 2 类的风险：$R(\alpha_2|x) = \lambda_{21}P(\omega_1|x) + \lambda_{22}P(\omega_2|x)$
    *   *(通常 $\lambda_{11}=\lambda_{22}=0$，即判对无损失)*
*   **决策规则**：**谁的风险小选谁**。比较 $R(\alpha_1|x)$ 和 $R(\alpha_2|x)$。
#### 3. 典型考题（高斯分布模型）
**题目**：已知两类满足正态分布 $N(\mu, \sigma^2)$，给定 $\mu_1, \sigma_1, \mu_2, \sigma_2$ 和先验 $P(\omega_1)$。求分类阈值。
**解题步骤**：
1.  列出等式：$P(x|\omega_1)P(\omega_1) = P(x|\omega_2)P(\omega_2)$ （分界点处概率相等）
2.  取对数：$\ln P(x|\omega_1) + \ln P(\omega_1) = \ln P(x|\omega_2) + \ln P(\omega_2)$
3.  代入高斯公式：$-\frac{(x-\mu_1)^2}{2\sigma_1^2} + \ln P(\omega_1) = -\frac{(x-\mu_2)^2}{2\sigma_2^2} + \ln P(\omega_2)$
4.  解方程求 $x$。
### 二、 朴素贝叶斯分类器
**核心思想**：在贝叶斯决策的基础上，**强行假设特征之间相互独立**（为了简化计算）。
#### 1. 模型公式
$$ P(\omega|x) \propto P(\omega) \times \prod_{j=1}^{d} P(x^{(j)}|\omega) $$
即：**后验得分 = 先验概率 $\times$ (特征1的似然 $\times$ 特征2的似然 $\dots$)**
#### 2. 计算步骤（三步走）
*   **Step 1 统计先验**：数数训练集中各类别的占比 $P(\omega)$。
*   **Step 2 统计似然**：数数在每一类中，各个特征值出现的概率 $P(x^{(j)}|\omega)$。
    *   *注意：如果某个概率为 0，需使用**拉普拉斯平滑**（分子+1，分母+类别数）。*
*   **Step 3 乘积比较**：对新样本，把对应的概率乘起来，谁大选谁。
### 三、 两者核心区别（必考对比）

| 维度 | **贝叶斯决策 (Bayesian Decision)** | **朴素贝叶斯 (Naive Bayes)** |
| :--- | :--- | :--- |
| **已知条件** | **题目直接给**概率分布公式和参数（上帝视角）。 | 给**原始数据表**，需要自己统计频率（凡人视角）。 |
| **特征处理** | 考虑特征间的**相关性**（如协方差矩阵）。 | 假设特征**相互独立**（直接连乘）。 |
| **计算难度** | 难（涉及多维积分或矩阵运算）。 | 易（简单的查表和乘法）。 |
| **适用场景** | 理论分析，已知物理模型。 | 文本分类、垃圾邮件过滤（高维稀疏数据）。 |

### 四、 典型例题演示
#### 例题 1：最小风险决策
**已知**：$P(\omega_1)=0.9, P(\omega_2)=0.1$。观察到 $x$，使得 $P(x|\omega_1)=0.2, P(x|\omega_2)=0.4$。
损失矩阵：判对损失 0；误判 1 为 2 损失 1；误判 2 为 1 损失 10。求决策。
**解**：
1.  **算后验分子（非归一化后验）**：
    *   $\omega_1$ 得分：$0.2 \times 0.9 = 0.18$
    *   $\omega_2$ 得分：$0.4 \times 0.1 = 0.04$
2.  **算风险（Risk）**：
    *   判为 1 的风险（错把2判为1）：$R(\alpha_1) = 损失(2\to1) \times \omega_2得分 = 10 \times 0.04 = 0.4$
    *   判为 2 的风险（错把1判为2）：$R(\alpha_2) = 损失(1\to2) \times \omega_1得分 = 1 \times 0.18 = 0.18$
3.  **决策**：
    因为 $0.18 < 0.4$，所以选择**判为 2 类**（虽然它概率低，但判错它代价太大）。
#### 例题 2：朴素贝叶斯预测
**训练集**：
(1) 帅, 性格好 -> 嫁
(2) 不帅, 性格好 -> 嫁
(3) 帅, 性格差 -> 不嫁
(4) 不帅, 性格差 -> 不嫁
**预测**：(不帅, 性格差) -> ?
**解**：
1.  **先验**：$P(嫁) = 0.5, P(不嫁) = 0.5$
2.  **似然（统计频率）**：
    *   在“嫁”中：$P(不帅|嫁) = 0.5, P(性格差|嫁) = 0$
    *   在“不嫁”中：$P(不帅|不嫁) = 0.5, P(性格差|不嫁) = 1.0$
3.  **计算得分**：
    *   $Score(嫁) = 0.5 \times 0.5 \times 0 = 0$
    *   $Score(不嫁) = 0.5 \times 0.5 \times 1.0 = 0.25$
4.  **结论**：$0.25 > 0$，预测**不嫁**。
### 包含拉普拉斯平滑的朴素贝叶斯
这是一道包含**拉普拉斯平滑（Laplace Smoothing）**的朴素贝叶斯经典考题。这通常出现在需要解决**“零概率”**问题的场景中。

---

### 一、 为什么要用拉普拉斯平滑？

**核心痛点：零概率问题**
在朴素贝叶斯中，我们需要连乘各个特征的条件概率。如果某个特征值在训练集的某个类别中**从来没出现过**（次数为 0）：
*   该特征的条件概率 $P(x|y) = 0$。
*   后果：**连乘的结果直接变为 0**，无论其他特征多么符合，整个类别的后验概率都变成 0。这显然是不合理的（没见过不代表不可能）。

**解决方法**：
给分子和分母都加上一个常数（通常是 1），强行让概率不为 0。

---

### 二、 平滑后的计算公式（必背）

$$ P(x^{(j)} | \omega_i) = \frac{N_{ij} + \lambda}{N_i + S_j \times \lambda} $$

*   **$N_{ij}$**：在 $\omega_i$ 类中，特征 $j$ 出现该取值的次数（原来的分子）。
*   **$N_i$**：$\omega_i$ 类的样本总数（原来的分母）。
*   **$S_j$**：特征 $j$ 可能取值的**种类数**（比如颜色有红、黄、绿3种，则 $S_j=3$）。
*   **$\lambda$**：平滑系数，拉普拉斯平滑规定 **$\lambda = 1$**。

---

### 三、 经典例题演示

**【题目】**
有一个水果训练集，包含 5 个样本：
1.  **苹果**：颜色=红，形状=圆
2.  **苹果**：颜色=红，形状=圆
3.  **苹果**：颜色=红，形状=圆
4.  **香蕉**：颜色=黄，形状=长
5.  **香蕉**：颜色=黄，形状=长

**测试样本**：**$x = \{颜色=红, 形状=长\}$**
**任务**：使用**拉普拉斯平滑**的朴素贝叶斯分类器判断 $x$ 是苹果还是香蕉。

---

**【解答步骤】**

#### **Step 1：分析特征空间（确定分母加多少）**
*   **类别**：苹果、香蕉。
*   **特征1（颜色）**：出现的取值有 {红, 黄}。虽然训练集没出现绿，但在考试中，如果没说其他颜色，通常认为只有出现的这 2 种。所以 **$S_{颜色} = 2$**。
*   **特征2（形状）**：出现的取值有 {圆, 长}。所以 **$S_{形状} = 2$**。

#### **Step 2：计算先验概率（通常也平滑，但有时考试简化不平滑，这里演示平滑版）**
*   总样本数 $N=5$，类别数 $K=2$。
*   $P(苹果) = \frac{3 + 1}{5 + 2} = \frac{4}{7}$
*   $P(香蕉) = \frac{2 + 1}{5 + 2} = \frac{3}{7}$

#### **Step 3：计算类条件概率（重点！）**

**A. 对于“苹果”类 ($N_{苹果}=3$)**
1.  **颜色=红**：
    *   训练集中苹果全是红的 (3次)。
    *   $P(红|苹果) = \frac{3 + 1}{3 + 2} = \frac{4}{5} = 0.8$
    *   *(注：分母加2是因为颜色有2种)*
2.  **形状=长**（**关键点：零概率**）：
    *   训练集中苹果全是圆的，长的出现 **0次**。
    *   $P(长|苹果) = \frac{0 + 1}{3 + 2} = \frac{1}{5} = 0.2$
    *   *(如果不平滑，这里是0，直接判死刑)*

**B. 对于“香蕉”类 ($N_{香蕉}=2$)**
1.  **颜色=红**：
    *   训练集中香蕉全是黄的，红的出现 **0次**。
    *   $P(红|香蕉) = \frac{0 + 1}{2 + 2} = \frac{1}{4} = 0.25$
2.  **形状=长**：
    *   训练集中香蕉全是长的 (2次)。
    *   $P(长|香蕉) = \frac{2 + 1}{2 + 2} = \frac{3}{4} = 0.75$

#### **Step 4：计算后验得分并比较**

*   **苹果的得分**：
    $$ Score(苹果) = P(苹果) \times P(红|苹果) \times P(长|苹果) $$
    $$ = \frac{4}{7} \times 0.8 \times 0.2 \approx 0.57 \times 0.16 = \mathbf{0.091} $$

*   **香蕉的得分**：
    $$ Score(香蕉) = P(香蕉) \times P(红|香蕉) \times P(长|香蕉) $$
    $$ = \frac{3}{7} \times 0.25 \times 0.75 \approx 0.43 \times 0.1875 = \mathbf{0.080} $$

#### **Step 5：结论**
因为 $0.091 > 0.080$，所以朴素贝叶斯分类器将该样本判为 **苹果**。

*(直观解释：虽然“长”更像香蕉，但“红色”这一特征在苹果中概率极高（几乎确定），而在香蕉中概率极低，且苹果的先验概率略大，综合下来险胜。)*
# SVM
## 核心公式，软间隔，核函数的参数
### 一、 SVM 的核心思想
1.  **目标**：寻找一个超平面，将两类数据分开，并使得**间隔（Margin）最大化**。
2.  **间隔**：分为**函数间隔**和**几何间隔**。
    *   几何间隔具有缩放不变性（即使权重 $w$ 和偏置 $b$ 同时扩大 10 倍，间隔不变），是我们真正优化的对象。
3.  **支持向量**：离超平面**最近**的那些样本点。
    *   **核心特性**：只有支持向量决定了超平面的位置，其他样本点（非支持向量）对模型没有影响。
### 二、 两个优化问题（必背公式）
#### 1. 原始问题 —— 最小化 $w$
目标是寻找一个超平面，最大化分类间隔 $\frac{2}{||w||}$，等价于最小化 $||w||^2$。
*   **目标函数**：$$ \min_{w, b} \frac{1}{2} ||w||^2 $$
*   **约束条件**（分类正确且在间隔外）：$$ s.t. \quad y_i(w^T x_i + b) \ge 1, \quad i=1,\dots,N $$
#### 2. 对偶问题 —— 求解 $\alpha$
引入拉格朗日乘子 $\alpha$，将约束问题转化为无约束问题。
*   **目标函数**（Max $\alpha$）：$$ \max_{\alpha} \sum_{i=1}^{N} \alpha_i - \frac{1}{2} \sum_{i,j=1}^{N} \alpha_i \alpha_j y_i y_j (x_i^T x_j) $$
*   **约束条件**：$$ \sum \alpha_i y_i = 0, \quad \alpha_i \ge 0 $$
*   **为什么要转对偶？**
    1.  更容易求解（二次规划问题）。
    2.  自然地引入**内积**形式，方便使用**核函数**。
### 三、 软间隔与松弛变量
当数据**线性不可分**（有噪声）时，允许少量样本犯错。
1.  **引入松弛变量 $\xi_i$**：代表第 $i$ 个样本违反约束的程度。
2.  **新目标函数**：$$ \min \frac{1}{2}||w||^2 + C \sum_{i=1}^{N} \xi_i $$
3.  **惩罚系数 $C$ 的作用**（重要考点）：
    *   **$C$ 大**：对错误零容忍 $\rightarrow$ **易过拟合**。
    *   **$C$ 小**：允许更多错误，追求更宽的间隔 $\rightarrow$ **易欠拟合**（但在噪声大时泛化能力更好）。
### 四、 核函数
当原始空间线性不可分时，将数据映射到**高维特征空间**使其可分。
1.  **定义**：$K(x, z) = \phi(x)^T \phi(z)$。
    *   **核技巧**：不需要显式计算高维向量 $\phi(x)$，直接在低维空间计算内积即可得到高维空间的结果。
2.  **常见核函数**：
    *   **线性核**：$x^T z$。适用于特征维数高的情况。决策边界只是直线
    *   **多项式核**：$(x^T z + 1)^d$。决策边界是多项式函数的形状；次数越高，边界越复杂
    *   **高斯核 (RBF)**：$\exp(-\gamma ||x-z||^2)$。**最常用**，映射到无穷维空间。
3.  **RBF 参数 $\gamma$ 的影响**：
    *   **$\gamma$ 大**：高斯分布尖锐，只看局部 $\rightarrow$ **过拟合**。
    *   **$\gamma$ 小**：高斯分布平坦，看整体 $\rightarrow$ **欠拟合**。
### 五、 KKT 条件与支持向量判定
通过 $\alpha_i$ 的值判断样本的角色：

| $\alpha_i$ 的值 | 样本位置 | 样本角色 |
| :--- | :--- | :--- |
| **$\alpha_i = 0$** | 在间隔外 ($y_i f(x_i) > 1$) | **非支持向量** (无用) |
| **$0 < \alpha_i < C$** | 恰好在间隔边界上 ($=1$) | **标准支持向量** |
| **$\alpha_i = C$** | 在间隔内部或分错 ($<1$) | **软间隔支持向量** (异常/噪声) |

### 六、 经典简答题例题
**问：为什么 SVM 对缺失数据敏感？**
**答**：SVM 的超平面完全由少数几个**支持向量**决定。如果缺失数据恰好是支持向量，或者缺失导致样本分布发生边缘改变，超平面会发生剧烈偏移，严重影响模型性能。
**问：SVM 与 逻辑回归 (LR) 的区别？**
**答**：
1.  **损失函数**：SVM 优化 Hinge Loss（合页损失）；LR 优化对数似然损失（交叉熵）。
2.  **稀疏性**：SVM 解是稀疏的（只依赖支持向量）；LR 解依赖所有样本。
3.  **原理**：SVM 是结构风险最小化（最大间隔）；LR 是经验风险最小化（概率模型）。
## 原始问题、对偶问题、硬间隔计算、核函数计算和直观求解
### 一、 SVM 的数学模型
#### 1. 原始问题
目标函数和约束条件，看上一节
#### 2. 对偶问题
将原始问题转化为对偶问题的动机、目标函数、约束条件，看上一节
#### 3. 转换关系（解出 $\alpha$ 后怎么求 $w, b$）
*   **求 $w$**：$$ w = \sum_{i=1}^{N} \alpha_i y_i x_i $$
*   **求 $b$**：选取一个**支持向量**（即 $\alpha_k > 0$ 的样本 $(x_k, y_k)$）：$$ b = y_k - w^T x_k $$
### 二、 硬间隔 SVM 求解流程（标准五步走）
**题目**：已知正样本点 $x_1=(3,3)^T, x_2=(4,3)^T$，负样本点 $x_3=(1,1)^T$。求硬间隔 SVM 超平面。($y_1=1, y_2=1, y_3=-1$)
#### 步骤 1：计算 Gram 矩阵（两两内积）
先算出所有样本点积 $x_i^T x_j$，简化后续书写。
*   $x_1 \cdot x_1 = 18, \quad x_1 \cdot x_2 = 21, \quad x_1 \cdot x_3 = 6$
*   $x_2 \cdot x_2 = 25, \quad x_2 \cdot x_3 = 7, \quad x_3 \cdot x_3 = 2$
#### 步骤 2：列出对偶目标函数
将内积代入目标函数公式：$$ L(\alpha) = (\alpha_1+\alpha_2+\alpha_3) - \frac{1}{2} [ 18\alpha_1^2 + 25\alpha_2^2 + 2\alpha_3^2 + 2(21\alpha_1\alpha_2 - 6\alpha_1\alpha_3 - 7\alpha_2\alpha_3) ] $$
*(注：$y_i y_j$ 的符号要乘进去，比如 $y_1 y_3 = -1$)*
#### 步骤 3：求解 $\alpha$（核心计算）
1.  **利用约束消元**：$\alpha_1(1) + \alpha_2(1) + \alpha_3(-1) = 0 \implies \alpha_3 = \alpha_1 + \alpha_2$。
2.  **代入目标函数**：把 $\alpha_3$ 换掉，对 $\alpha_1, \alpha_2$ 求偏导并令为 0。
3.  **解方程组**：
    *   如果解出 $\alpha_i < 0$，说明假设错误（该点不是支持向量）。
    *   **修正**：令该负值的 $\alpha_i = 0$，重新求解剩余变量。
    *   本题解得：$\alpha_1 = 0.25, \alpha_2 = 0, \alpha_3 = 0.25$。
#### 步骤 4：计算 $w$
$$ w = 0.25(1)(3,3) + 0 + 0.25(-1)(1,1) = (0.5, 0.5)^T $$
#### 步骤 5：计算 $b$
选用支持向量 $x_1$ ($\alpha_1 > 0$)：
$$ b = 1 - (0.5 \times 3 + 0.5 \times 3) = -2 $$
**最终超平面**：$0.5x^{(1)} + 0.5x^{(2)} - 2 = 0$
### 三、 核函数计算与非线性映射
**考点**：考察通过观察法求 $w$，以及核函数的定义。
**题目**：映射 $\phi(x) = [1, x_1, x_2, x_1 x_2]^T$。求核函数 $K(x, z)$。
**解法**：
1.  **定义**：$K(x, z) = \phi(x) \cdot \phi(z)$ （映射后的点积）。
2.  **写出向量**：
    $\phi(x) = [1, x_1, x_2, x_1 x_2]$
    $\phi(z) = [1, z_1, z_2, z_1 z_2]$
3.  **点积计算**：$$ K(x, z) = 1\cdot1 + x_1 z_1 + x_2 z_2 + (x_1 x_2)(z_1 z_2) $$
    *(这就是多项式核函数展开后的形式)*
**观察法求 $w$ 技巧**：
如果题目问“求高维空间参数 $w$”，且数据是**异或分布**（XOR）：
*   观察映射后的向量，通常只有**交叉项（如 $x_1 x_2$）**那一维能把正负样本符号分开。
*   直接令该维度的权重为 1（或 -1），其余为 0，即可得到 $w$。
# 决策树
## 信息增益，基尼系数，决策树的优缺点，过拟合处理
### 一、 决策树节点分裂计算
决策树的核心是：**在每个节点选择一个最优特征进行分裂。**
#### 1. ID3 算法（基于信息增益）
*   **核心指标**：**信息熵（Entropy）**。衡量数据集的“混乱程度”或“不确定性”。
*   **计算步骤（三步走）**：
    1.  **算总熵**：$$ Ent(D) = -\sum_{k=1}^{K} p_k \log_2 p_k $$
        *   $p_k$：第 $k$ 类样本在数据集 $D$ 中的占比。
    2.  **算条件熵**（按特征 A 分裂后的加权平均熵）：$$ Ent(D|A) = \sum_{v} \frac{|D^v|}{|D|} Ent(D^v) $$
        *   $|D^v|$：特征 A 取值为 $v$ 的样本数。
        *   $Ent(D^v)$：该分支子集的信息熵。
    3.  **算信息增益**：$$ Gain(D, A) = Ent(D) - Ent(D|A) $$
    *   **决策规则**：选择 **Gain 最大** 的特征进行分裂。
#### 2. CART 算法（基于基尼指数）
*   **核心指标**：**基尼指数（Gini Index）**。衡量数据集的“纯度”（越小越纯）。
*   **计算步骤**：
    1.  **算基尼值**：$$ Gini(D) = 1 - \sum_{k=1}^{K} p_k^2 $$
    2.  **算分裂后的基尼指数**：$$ Gini\_index(D, A) = \sum_{v} \frac{|D^v|}{|D|} Gini(D^v) $$
    *   **决策规则**：选择 **Gini_index 最小** 的特征进行分裂。
### 二、 决策树的优缺点与过拟合处理
#### 1. 决策树的优缺点
**优点**：
1.  **可解释性强**：生成的规则类似于人类思维（如果...那么...），易于理解。
2.  **数据预处理简单**：不需要归一化，能处理离散和连续特征。
3.  **非线性分类**：能处理复杂的非线性关系。
**缺点**：
4.  **容易过拟合**：如果不限制生长，树会变得非常复杂，死记硬背训练数据。
5.  **不稳定**：数据的一点微小变化可能导致树结构的剧烈改变。
6.  **ID3 的偏好**：信息增益倾向于选择**取值较多**的特征（如身份证号），但这通常是无效的。
#### 2. 信息增益的优缺点
*   **优点**：概念清晰，能有效降低数据不确定性。
*   **缺点**：**偏向于取值较多的特征**。为了解决这个问题，**C4.5 算法**引入了**信息增益率**（增益除以特征的固有熵）来进行修正。
#### 3. 如何防止过拟合？（核心简答题）
主要策略有两类：**剪枝** 和 **限制结构参数**。
1.  **剪枝 (Pruning)**：
    *   **预剪枝**：**边建树边刹车**。在分裂前评估（如验证集精度是否提升），若无提升则停止分裂。
        *   *特点*：快，但可能导致欠拟合（视力短浅）。
    *   **后剪枝**：**先建满再修剪**。树完全生长后，自底向上评估，若剪掉子树能提高验证集精度，则替换为叶节点。
        *   *特点*：慢，但泛化能力通常更好。
2.  **限制结构参数**：
    *   限制**最大深度** (Max Depth)。
    *   限制**叶节点最小样本数** (Min Samples Leaf)。
    *   限制**分裂最小增益阈值**。
3.  **集成学习**：使用 **随机森林**（多棵树投票）来降低方差。
### 三、 真题计算演示（ID3）
**题目**：
数据 $D$（6个样本）：
*   **是(Yes)**：4个 (分布：2/3)
*   **否(No)**：2个 (分布：1/3)
特征 A（天气）：
*   **晴**：2个（1是，1否）
*   **雨**：4个（3是，1否）
**求特征 A 的信息增益。**
**解**：
1.  **算总熵**：$$ Ent(D) = -(\frac{2}{3}\log_2\frac{2}{3} + \frac{1}{3}\log_2\frac{1}{3}) \approx 0.918 $$

2.  **算条件熵**：
    *   **晴 (2个)**：$p_+ = 0.5, p_- = 0.5$。
        $Ent(晴) = -(0.5\log 0.5 + 0.5\log 0.5) = 1.0$
    *   **雨 (4个)**：$p_+ = 0.75, p_- = 0.25$。
        $Ent(雨) = -(0.75\log 0.75 + 0.25\log 0.25) \approx 0.811$
    *   **加权求和**：$$ Ent(D|A) = \frac{2}{6} \times 1.0 + \frac{4}{6} \times 0.811 \approx 0.333 + 0.541 = 0.874 $$
3.  **算增益**：$$ Gain(D, A) = 0.918 - 0.874 = \mathbf{0.044} $$
# 集成学习
## 集成学习核心思想，Bagging和Boosting对比，AdaBoost算法
### 一、 集成学习核心理论
#### 1. 核心思想
**“三个臭皮匠，顶个诸葛亮”**。通过构建并结合多个**基学习器**（弱分类器）来完成学习任务。
*   **目的**：通过组合多个模型，降低整体的**偏差**或**方差**，从而提高模型的**泛化能力**。
*   **原理**：
    *   **误差抵消**：不同分类器的错误可能相互抵消（降低方差）。
    *   **空间覆盖**：单个模型可能只能学到数据的一部分特征，组合后能逼近真实分布（降低偏差）。
#### 2. Bagging 与 Boosting 的区别

| 维度 | **Bagging (代表：随机森林)** | **Boosting (代表：AdaBoost)** |
| :--- | :--- | :--- |
| **训练样本** | **随机采样** (Bootstrap 有放回抽样)。 | **不变**，但**权重改变** (分错的样本权重变大)。 |
| **基学习器关系** | **并行**生成，相互独立。 | **串行**生成，后一个依赖前一个。 |
| **权重分配** | 所有基学习器权重**相等** (投票/平均)。 | 误差小的基学习器**权重高** (话语权大)。 |
| **主要作用** | 降低**方差** (Variance)，防过拟合。 | 降低**偏差** (Bias)，提升拟合能力。 |

### 二、 AdaBoost 算法计算流程（三步走）
AdaBoost 的核心逻辑是：**“关注错题本”**。
假设当前是第 $t$ 轮迭代，总样本数 $N$。
*   $D_t$：当前样本权重分布。
*   $h_t(x)$：当前轮训练出的弱分类器。
*   $\hat{y}_i$：预测值，$y_i$：真实值。
#### Step 0：初始化
第一轮开始前，所有样本权重相等：$w_{1,i} = \frac{1}{N}$
#### Step 1：算误差率 $\epsilon_t$
**口诀：只加分错的。**
在当前权重下，找出所有分类错误的样本，把它们的权重加起来。
$$ \epsilon_t = \sum_{i: 分错的} w_{t,i} $$
*(注：如果未加权错误率 $>0.5$，算法通常停止或反转符号)*
#### Step 2：算分类器权重 $\alpha_t$
**口诀：误差越小，话语权越大。**$$ \alpha_t = \frac{1}{2} \ln \left( \frac{1 - \epsilon_t}{\epsilon_t} \right) $$
#### Step 3：算新样本权重 $D_{t+1}$
**口诀：错的变大，对的变小，最后归一化。**
1.  **调整权重**：
    *   分**对**的样本：$w_{new} = w_{old} \times e^{-\alpha_t}$ （缩小）
    *   分**错**的样本：$w_{new} = w_{old} \times e^{\alpha_t}$ （放大）
2.  **归一化**：
    算出所有 $w_{new}$ 的总和 $Z_t$，每个新权重都要除以 $Z_t$，确保和为 1。$$ w_{t+1, i} = \frac{w_{new}}{Z_t} $$
### 三、 真题手算演示
**题目**：
样本：A(+1), B(+1), C(-1), D(-1)。初始权重均为 0.25。
分类器 $h_1$ 预测结果：A(+1), B(+1), C(-1), **D(+1)**。
*(可见只有 D 分错了)*
**求解第一轮更新后的权重。**
**解**：
**1. 算误差率 $\epsilon_1$**：
只有 D 分错，D 的权重是 0.25。$$ \epsilon_1 = 0.25 $$
**2. 算分类器权重 $\alpha_1$**：$$ \alpha_1 = \frac{1}{2} \ln \left( \frac{1 - 0.25}{0.25} \right) = \frac{1}{2} \ln 3 \approx 0.55 $$
**3. 算新样本权重 $D_2$**：
先算两个乘数因子：
*   **分错放大**：$e^{\alpha_1} = \sqrt{3} \approx 1.732$
*   **分对缩小**：$e^{-\alpha_1} = 1/\sqrt{3} \approx 0.577$
*   **调整**：
    *   A, B, C (对)：$0.25 \times 0.577 = 0.144$
    *   D (错)：$0.25 \times 1.732 = 0.433$
*   **归一化因子 $Z$**：$$ Z = 0.144 \times 3 + 0.433 \approx 0.865 $$
*   **最终权重**：
    *   $w_A = w_B = w_C = 0.144 / 0.865 \approx \mathbf{0.167}$
    *   $w_D = 0.433 / 0.865 \approx \mathbf{0.500}$
**检查点**：分错的 D 权重变大 (0.25 -> 0.5)，分对的权重变小，且总和为 1。计算正确。

# PCA
## 求均值按哪个维度，自相关和协方差矩阵，特征值和特征向量
### 一、 数据的矩阵表示
在手算题目中（如模式识别、线性代数背景），务必遵循以下“黄金法则”，否则矩阵乘法维度会对不上：

1.  **列（Column）代表样本**：$N$ 个样本。
2.  **行（Row）代表特征**：$d$ 个维度。
3.  **矩阵形态**：$X$ 是一个 $d \times N$ 的矩阵。

$$ X = \begin{bmatrix} \text{样本1的特征1} & \text{样本2的特征1} & \dots \\ \text{样本1的特征2} & \text{样本2的特征2} & \dots \end{bmatrix} $$
> 当然题目也可能反过来，关键是明确是对**特征维度**求平均、中心化
### 二、 核心统计量计算
#### 1. 均值向量
*   **对谁求平均？** 对 **行** 求平均（横向压缩）。即把所有样本在同一个特征上的数值加起来除以 $N$。
*   **结果**：得到一个 $d \times 1$ 的列向量 $\mu$。
#### 2. 去中心化
*   **操作**：用原始矩阵 $X$ 的每一列（每个样本），减去均值向量 $\mu$。
*   **目的**：将数据的中心移到坐标原点，简化后续计算。
#### 3. 自相关矩阵与协方差矩阵（区分与联系）
*   **自相关矩阵 R**：
    *   **定义**：基于**原始数据**（未减均值）计算。
    *   **公式**：$R = \frac{1}{N} X X^T$
    *   **维度检查**：$(d \times N) \times (N \times d) = d \times d$。结果必须是方阵。
    *   **物理意义**：包含数据的能量（方差）和位置信息（均值）。
*   **协方差矩阵 $\Sigma$**：
    *   **定义**：基于**去中心化数据**（减去均值后）计算。
    *   **公式**：$\Sigma = \frac{1}{N} (X-\mu)(X-\mu)^T$
        *   *(注：统计学常用 N-1 为分母，但考试手算为了整除通常用 N。总之用好算的那个)*
    *   **物理意义**：只描述特征之间的相关性和离散程度，与数据位置无关。
*   **两者关系**：$$ \Sigma = R - \mu \mu^T $$
    *   **特例**：如果数据均值为 0（已经中心化），则 **协方差矩阵 = 自相关矩阵**。
### 三、 特征值与特征向量（解方程核心）
对于求出的 $d \times d$ 矩阵（设为 $A$，可以是 $R$ 或 $\Sigma$）：
#### 1. 特征方程
$$ (A - \lambda E) x = 0 $$
*   $A$：矩阵。
*   $\lambda$：特征值（标量）。
*   $E$：单位矩阵。
*   $x$：特征向量（非零列向量）。
#### 2. 求解步骤
1.  **求特征值 $\lambda$**：
    令行列式 $|A - \lambda E| = 0$。解这个关于 $\lambda$ 的一元多次方程。
    *   *几何意义*：只有行列式为0（矩阵不可逆），方程才有非零解 $x$。
2.  **求特征向量 $x$**：
    将求出的 $\lambda$ 代回方程 $(A - \lambda E) x = 0$，解出基础解系。
3.  **归一化（重要！）**：
    PCA 要求基向量模长为 1。将求出的向量除以它的模长。$$ x_{norm} = \frac{x}{||x||} $$
## PCA的推导，最大方差和最小误差重构
### 前置假设（所有推导的前提）
1.  样本集为 $X = \{x_1, x_2, \dots, x_N\}$。
2.  数据已经过**去中心化**处理，即均值为 0，$\sum x_i = 0$。
3.  目标是找到一个投影方向向量 $u$，它是单位向量，即 $u^T u = 1$。
### 思路一：最大方差法
**核心思想**：
数据投影到某个方向后，点越分散（方差越大），保留的信息越多。
**推导步骤**：
1.  **计算投影**：样本 $x_i$ 在方向 $u$ 上的投影长度（坐标）为 $x_i^T u$。
2.  **计算方差**：
    因为均值为 0，投影后的方差就是坐标平方的均值：$$ S^2 = \frac{1}{N} \sum_{i=1}^{N} (x_i^T u)^2 = \frac{1}{N} u^T (\sum x_i x_i^T) u = u^T \Sigma u $$
    其中 $\Sigma$ 是协方差矩阵。
3.  **建立优化目标**：$$ \max u^T \Sigma u, \quad \text{s.t.} \quad u^T u = 1 $$
4.  **拉格朗日乘子法求解**：
    构造函数：$L(u, \lambda) = u^T \Sigma u - \lambda(u^T u - 1)$
    对 $u$ 求导并令为 0：$$ \frac{\partial L}{\partial u} = 2\Sigma u - 2\lambda u = 0 \quad \Rightarrow \quad \Sigma u = \lambda u $$
**结论**：
最优投影方向 $u$ 就是协方差矩阵 $\Sigma$ 的**特征向量**，对应的方差就是**特征值 $\lambda$**。要方差最大，就选最大的特征值。
### 思路二：最小重构误差法（信号重构）
**核心思想**：
把高维数据压缩到低维后再恢复（重构），希望恢复后的数据和原始数据差别（误差）最小。
**推导步骤**：
1.  **定义重构信号**：
    原始向量 $x_i$ 在方向 $u$ 上的投影是 $(x_i^T u) u$。这就是我们重构出的近似向量 $\hat{x}_i$。
2.  **定义损失函数**（重构误差）：
    我们要最小化原始向量与重构向量之间的欧氏距离平方和：$$ J = \sum_{i=1}^{N} \|x_i - \hat{x}_i\|^2 $$
3.  **几何分解（勾股定理）**：
    对于任何向量，**原始模长平方 = 投影分量模长平方 + 垂直分量（误差）模长平方**。
    即：$\|x_i\|^2 = \|\hat{x}_i\|^2 + \|\text{error}\|^2$。
    所以：$\|\text{error}\|^2 = \|x_i\|^2 - \|\hat{x}_i\|^2$。
4.  **转化目标**：$$ \min \sum (\|x_i\|^2 - \|\hat{x}_i\|^2) $$
    因为原始数据模长 $\sum \|x_i\|^2$ 是固定的常数，**最小化误差等价于最大化重构向量的模长**$$ \max \sum_{i=1}^{N} \|\hat{x}_i\|^2 = \max \sum_{i=1}^{N} (x_i^T u)^2 $$
5.  **回归殊途同归**：
    你会发现，上一步得到的式子 $\sum (x_i^T u)^2$ 正好就是**最大方差法**中的方差公式（相差一个常数系数 $N$）。
    后续求解同样使用拉格朗日乘子法，结果一致。
**结论**：
让丢失的信息（误差）最小，等价于让保留的信息（方差/投影模长）最大。解依然是协方差矩阵的**特征向量**。
### 考试总结（一句话区别）
*   **最大方差法**：侧重于“投影后的数据分得越开越好”。
*   **最小重构误差法**：侧重于“投影后的数据丢掉的细节越少越好”。
*   **数学结果**：完全一致，都是求协方差矩阵的特征值和特征向量。
## PCA计算流程
### 核心解题步骤
#### 1. 数据整理（矩阵化）
*   将给定的样本写成矩阵形式 $X$。
*   **注意**：考试中通常**一列代表一个样本**，一行代表一个特征。
    $$ X = [x_1, x_2, \dots, x_N] $$
#### 2. 计算均值与去中心化
*   **求均值 $\mu$**：对每一行（特征）分别求平均值。
*   **去中心化**：用原始矩阵 $X$ 的每一列减去均值向量 $\mu$，得到新矩阵 $\tilde{X}$。
    *   *注：如果题目给的数据均值已经是0，这步可跳过。*
#### 3. 计算协方差矩阵 $\Sigma$
*   公式：$\Sigma = \frac{1}{N} \tilde{X} \tilde{X}^T$
*   **手算技巧**：利用矩阵乘法，行乘行。
    *   $\Sigma$ 的第 $(i, j)$ 个元素 = 第 $i$ 行向量与第 $j$ 行向量的内积，再除以样本数 $N$。
#### 4. 求特征值与特征向量
*   **求特征值 $\lambda$**：解方程 $|\Sigma - \lambda E| = 0$。
*   **求特征向量 $v$**：将 $\lambda$ 代回方程 $(\Sigma - \lambda E)v = 0$ 求解。
*   **归一化（必做！）**：将求出的特征向量除以其模长，得到单位向量。
#### 5. 投影与降维
*   **选主成分**：选取**最大**的特征值对应的特征向量 $u_1$ 作为第一主成分方向。
*   **算投影（降维坐标）**：
    $$ y = u_1^T (x - \mu) $$
    即：归一化特征向量 $\cdot$ 去中心化的样本。
*   **算贡献率**：$\lambda_1 / (\lambda_1 + \lambda_2 + \dots)$。
### 真题计算示例
**题目**：样本 $x_1=(1, -1)^T, x_2=(3, 3)^T, x_3=(-1, 1)^T, x_4=(-3, -3)^T$。求第一主成分方向及 $x_2$ 的投影。
**解答**：
1.  **均值**：$\mu = (0, 0)^T$。（数据已中心化）
2.  **协方差矩阵**：
    *   行1 $\cdot$ 行1 = $1^2+3^2+(-1)^2+(-3)^2 = 20$
    *   行2 $\cdot$ 行2 = $(-1)^2+3^2+1^2+(-3)^2 = 20$
    *   行1 $\cdot$ 行2 = $1(-1)+3(3)+(-1)1+(-3)(-3) = 16$
    *   除以 $N=4$：$\Sigma = \begin{bmatrix} 5 & 4 \\ 4 & 5 \end{bmatrix}$
3.  **求特征值**：
    $|\Sigma - \lambda E| = (5-\lambda)^2 - 16 = 0 \Rightarrow \lambda_1 = 9, \lambda_2 = 1$。
4.  **求特征向量（对应 $\lambda_1=9$）**：
    $\begin{bmatrix} -4 & 4 \\ 4 & -4 \end{bmatrix} v = 0 \Rightarrow v = [1, 1]^T$。
    **归一化**：$u_1 = [1/\sqrt{2}, 1/\sqrt{2}]^T \approx [0.707, 0.707]^T$。
5.  **投影**：
    $x_2$ 的投影坐标 $= u_1^T x_2 = 0.707 \times 3 + 0.707 \times 3 = 4.242$。
    > 如果还想要求投影后的坐标，就把模长乘以对应方向的方向向量
    
# 聚类
## K-means算法及其优缺点，最小最大距离法
### 一、 K-Means 算法
#### 1. 计算步骤（标准四步走）
K-Means 是一种迭代式算法，旨在将数据集划分为 $K$ 个簇。

1.  **初始化**：随机选择 $K$ 个样本点作为初始的**聚类中心**。
2.  **分配**：遍历每个样本点，计算它到 $K$ 个中心的距离，将其分配到**距离最近**的簇中。
3.  **更新**：对于每一个簇，重新计算其所有成员点的**均值**，并将该均值作为新的聚类中心。
4.  **迭代**：重复步骤 2 和 3，直到中心不再变化或达到最大迭代次数。
#### 2. 优缺点（必考简答）
*   **优点**：
    *   原理简单，实现容易，收敛速度快。
    *   可解释性强。
*   **缺点**：
    *   需预先指定 **$K$ 值**。
    *   对**初始中心**敏感（容易陷入局部最优）。
    *   对**噪声和离群点**敏感（均值易被拉偏）。
    *   只能处理**球状**分布的数据。
### 二、 最大最小距离法（Max-Min Distance Algorithm）
这是 K-Means 的一种改进（试探法），主要用于**自动确定聚类中心的个数**。
#### 1. 核心逻辑
*   先找最远的两个点定下基调。
*   再找“离所有现有中心都足够远”的点作为新中心。
*   直到找不到足够远的点了，就停止。
#### 2. 计算步骤（手算题流程）
1.  **选 $Z_1$**：题目指定或随机选第一个中心。
2.  **选 $Z_2$**：找出离 $Z_1$ **距离最远**的样本，作为第二个中心。
3.  **定阈值 $T$**：计算 $Z_1, Z_2$ 的距离 $D_{12}$，阈值 $T = \theta \times D_{12}$。（$\theta$ 由题目给定）。
4.  **找候选 (Max-Min)**：
    *   对剩余每个点，算出它到最近中心的距离（Min）。
    *   从这些距离中选一个**最大**的（Max），记为 $D_{max}$，对应样本为候选点。
5.  **判断与迭代**：
    *   若 $D_{max} > T$：该点成为新中心 $Z_3$。更新阈值 $T$（通常取中心间平均距离的 $\theta$ 倍），重复步骤 4。
    *   若 $D_{max} \le T$：停止寻找，现有中心即为最终中心。
6.  **归类**：将剩余样本按就近原则分配给各中心。
### 三、 真题手算示例（最大最小距离法）
**题目**：样本 $\{0, 2, 12\}$。指定 $Z_1=0, \theta=0.5$。求聚类结果。
**解答**：
1.  **选 $Z_2$**：
    *   2 到 0 距离为 2。
    *   12 到 0 距离为 12。
    *   最远的是 12，所以 **$Z_2 = 12$**。
2.  **算阈值**：
    *   $D_{12} = 12$。
    *   $T = 0.5 \times 12 = \mathbf{6}$。
3.  **找 $Z_3$**：
    *   剩下一个样本 2。
    *   它到 $\{0, 12\}$ 的最小距离是 $2$（离0近）。
    *   $D_{max} = 2$。
4.  **判断**：
    *   $D_{max} (2) < T (6)$。
    *   不够远，不选新中心。算法结束。
5.  **结果**：
    *   中心为 $\{0, 12\}$。
    *   样本 2 归入类 1（中心0）。
    *   最终聚类：$\{0, 2\}$ 和 $\{12\}$。
### 四、 真题手算示例（K-Means）
**题目**：
有一组一维样本点 $D = \{1, 2, 6, 7\}$。
设定聚类个数 $K=2$。
**初始中心**：选择前两个点作为初始中心，即 $\mu_1 = 1, \mu_2 = 2$。
请手动计算 K-Means 的迭代过程，直到收敛。
**解答**：
#### 第 1 轮迭代
1.  **分配样本（计算距离）**：
    *   点 1：到 $\mu_1(1)$ 距离 0，到 $\mu_2(2)$ 距离 1。$\to$ **归入簇 1**。
    *   点 2：到 $\mu_1(1)$ 距离 1，到 $\mu_2(2)$ 距离 0。$\to$ **归入簇 2**。
    *   点 6：到 $\mu_1(1)$ 距离 5，到 $\mu_2(2)$ 距离 4。$\to$ **归入簇 2**。
    *   点 7：到 $\mu_1(1)$ 距离 6，到 $\mu_2(2)$ 距离 5。$\to$ **归入簇 2**。
    *   **当前划分**：簇 1 = $\{1\}$，簇 2 = $\{2, 6, 7\}$。
2.  **更新中心（计算均值）**：
    *   $\mu_1^{new}$：只有 $\{1\}$，均值 = **1**。
    *   $\mu_2^{new}$：$\{2, 6, 7\}$ 的平均值 = $(2+6+7)/3 = 15/3 = \mathbf{5}$。
#### 第 2 轮迭代
*(使用新中心 $\mu_1=1, \mu_2=5$)*
1.  **分配样本**：
    *   点 1：到 1 距离 0，到 5 距离 4。$\to$ **归入簇 1**。
    *   点 2：到 1 距离 1，到 5 距离 3。$\to$ **归入簇 1**（**注意：它变心了！**）。
    *   点 6：到 1 距离 5，到 5 距离 1。$\to$ **归入簇 2**。
    *   点 7：到 1 距离 6，到 5 距离 2。$\to$ **归入簇 2**。
    *   **当前划分**：簇 1 = $\{1, 2\}$，簇 2 = $\{6, 7\}$。
2.  **更新中心**：
    *   $\mu_1^{new}$：$\{1, 2\}$ 的平均值 = $1.5$。
    *   $\mu_2^{new}$：$\{6, 7\}$ 的平均值 = $6.5$。
#### 第 3 轮迭代（验证收敛）
*(使用新中心 $\mu_1=1.5, \mu_2=6.5$)*
*   点 1, 2 显然离 1.5 更近。
*   点 6, 7 显然离 6.5 更近。
*   **样本归属没有发生变化**，算法收敛。
**最终结果**：
*   聚类中心：$1.5$ 和 $6.5$。
*   分类结果：$\{1, 2\}$ 一类，$\{6, 7\}$ 一类。
## EM算法以及用它来求解GMM的过程
### 一、 核心概念与关系
1.  **GMM (高斯混合模型)**：
    *   这是一个**概率聚类模型**。它假设数据是由 $K$ 个高斯分布（Normal Distribution）混合而成的。
    *   **参数**：每个聚类都有三个参数：**权重 $\pi_k$**、**均值 $\mu_k$**、**协方差 $\Sigma_k$**。
2.  **EM 算法 (期望最大化)**：
    *   这是一个**迭代优化算法**。专门用来解决**含有隐变量**（Hidden Variable）的参数估计问题。
    *   **关系**：GMM 是模型，EM 是解这个模型的算法。因为我们不知道每个样本具体属于哪个高斯分布（这就是隐变量），所以无法直接求导求解，必须用 EM。
### 二、 EM 求解 GMM 的流程
EM 算法通过迭代两个步骤直到收敛：**E步（猜概率）** 和 **M步（算参数）**。
#### E步 (Expectation)：算“责任”
固定当前的参数（均值、方差、权重），计算每个样本 $x_i$ 属于第 $k$ 个聚类的**后验概率**（即“责任” $\gamma_{ik}$）。
$$ \gamma_{ik} = P(z_i=k | x_i) = \frac{\pi_k \cdot N(x_i | \mu_k, \Sigma_k)}{\sum_{j=1}^{K} \pi_j \cdot N(x_i | \mu_j, \Sigma_j)} $$
*   **直觉**：算出点 $x_i$ 在第 $k$ 个高斯分布下的“高度”，再归一化（占比多少）。
#### M步 (Maximization)：更新参数
利用 E 步算出的责任 $\gamma_{ik}$ 作为**权重**，重新计算 GMM 的参数（加权平均）。
1.  **更新均值**：$$ \mu_k^{new} = \frac{\sum_{i=1}^{N} \gamma_{ik} x_i}{\sum_{i=1}^{N} \gamma_{ik}} $$
    *(直觉：算重心，那个点属于我的概率大，它对我的重心影响就大)*
2.  **更新权重**：$$ \pi_k^{new} = \frac{\sum_{i=1}^{N} \gamma_{ik}}{N} $$
    *(直觉：属于第 k 类的总人数占比)*
3.  **更新协方差**：$$ \Sigma_k^{new} = \frac{\sum_{i=1}^{N} \gamma_{ik} (x_i - \mu_k^{new})(x_i - \mu_k^{new})^T}{\sum_{i=1}^{N} \gamma_{ik}} $$
### 三、 手算真题演示（1D 简化版）
**题目**：
数据 $x = \{1, 2, 20\}$。聚成 2 类 ($K=2$)。
初始参数：$\mu_1=1, \mu_2=20$。假设方差固定 $\sigma^2=1$，权重相等 $\pi_1=\pi_2=0.5$。
**求第一轮 EM 迭代后的均值 $\mu_1^{new}$。**
**解**：
**1. E步（算责任 $\gamma$）**
我们需要比较每个点离哪个中心近（概率密度高）。
*   **$x=1$**：离 $\mu_1(1)$ 距离 0，离 $\mu_2(20)$ 远。$\gamma_{1,1} \approx 1, \gamma_{1,2} \approx 0$。
*   **$x=2$**：离 $\mu_1(1)$ 距离 1，离 $\mu_2(20)$ 距离 18。显然属于类 1。$\gamma_{2,1} \approx 1, \gamma_{2,2} \approx 0$。
*   **$x=20$**：离 $\mu_2(20)$ 近。$\gamma_{3,1} \approx 0, \gamma_{3,2} \approx 1$。
**2. M步（更新均值）**
利用 $\gamma_{i,1}$ 更新 $\mu_1$：
$$ \mu_1^{new} = \frac{1.0 \times 1 + 1.0 \times 2 + 0.0 \times 20}{1.0 + 1.0 + 0.0} = \frac{3}{2} = 1.5 $$
### 四、 简答题：K-Means 与 GMM 的区别

| 维度 | **K-Means** | **GMM (高斯混合模型)** |
| :--- | :--- | :--- |
| **分配方式** | **硬分配** (Hard)：非黑即白 (0或1)。 | **软分配** (Soft)：给出概率 (如 0.7)。 |
| **簇形状** | 只能是**球形** (欧氏距离)。 | 可以是**椭圆形** (由协方差矩阵控制)。 |
| **参数** | 只有均值。 | 均值、方差、权重。 |
| **本质** | 坐标下降法 (EM的特例)。 | EM 算法。 |
# 强化学习
### 一、 核心概念与公式
#### 1. 贝尔曼方程（状态价值）
描述了当前状态价值与未来状态价值的关系。$$ V(s) = \max_{a} [ R(s,a) + \gamma \sum_{s'} P(s'|s,a) V(s') ] $$
*   $R(s,a)$：即时奖励。
*   $\gamma$：折扣因子（0到1之间，越接近1越看重未来）。
*   $V(s')$：下一状态的价值。
*   **简化版**（确定性环境，P=1）：$$ V(s) = \max_{a} [ R(s,a) + \gamma V(s') ] $$
#### 2. Q-Learning 更新公式
无模型算法，直接更新动作价值函数 Q。
$$ Q_{new}(s,a) = Q_{old}(s,a) + \alpha [ \underbrace{R + \gamma \max_{a'} Q(s', a')}_{\text{目标值}} - Q_{old}(s,a) ] $$
*   $\alpha$：学习率。
*   **核心逻辑**：用“现实”（奖励+下一时刻最好的未来）去修正“估计”（当前的Q值）。
### 二、 价值迭代（Value Iteration）手算流程
**逻辑**：不断更新 $V$ 值，直到收敛。策略隐式地由 $\max$ 操作决定。
**题目**：
两个状态 $s_1, s_2$。动作 $a$ 固定（无需选择）。
*   $s_1 \xrightarrow{a} s_2$，奖励 4。
*   $s_2 \xrightarrow{a} s_1$，奖励 1。
*   折扣 $\gamma=0.5$。初始 $V_0(s_1)=0, V_0(s_2)=0$。
**求前两轮迭代的价值。**
**解答**：
**第 1 轮 ($k=1$)**：
代入公式 $V_{k+1}(s) = R + \gamma V_k(s')$。
*   $V_1(s_1) = 4 + 0.5 \times V_0(s_2) = 4 + 0 = \mathbf{4}$
*   $V_1(s_2) = 1 + 0.5 \times V_0(s_1) = 1 + 0 = \mathbf{1}$
**第 2 轮 ($k=2$)**：
利用上一轮的 $V_1$ 计算。
*   $V_2(s_1) = 4 + 0.5 \times V_1(s_2) = 4 + 0.5 \times 1 = \mathbf{4.5}$
*   $V_2(s_2) = 1 + 0.5 \times V_1(s_1) = 1 + 0.5 \times 4 = \mathbf{3}$
### 三、 Q-Learning 手算流程
**逻辑**：给一步数据，更新一次 Q 表。
**题目**：
状态 $A, B$。动作 $Left, Right$。
当前 Q 表（全为0）：$Q(A, \cdot)=0, Q(B, \cdot)=0$。
**经历了一步**：在状态 $A$ 采取动作 $Right$，得到奖励 $r=2$，进入状态 $B$。
已知：$\alpha=0.1, \gamma=0.9$。假设状态 $B$ 的最优动作价值估计 $\max Q(B, a')=0$（因为表是空的）。
**求更新后的 $Q(A, Right)$。**
**解答**：
1.  **算目标值**：
    $Target = r + \gamma \max_{a'} Q(B, a') = 2 + 0.9 \times 0 = 2$
2.  **算误差**：
    $Error = Target - Q_{old}(A, Right) = 2 - 0 = 2$
3.  **更新 Q 值**：
    $Q_{new} = Q_{old} + \alpha \times Error = 0 + 0.1 \times 2 = \mathbf{0.2}$
### 四、 策略迭代 vs 价值迭代

| 维度 | **策略迭代** | **价值迭代** |
| :--- | :--- | :--- |
| **步骤** | **评估**（算准V） + **改进**（更新策略）。两步交替。 | 只有一步：直接用贝尔曼最优公式更新 V。 |
| **策略更新** | 显式更新策略 $\pi$。 | 隐式更新（由 max 操作隐含）。 |
| **收敛速度** | 迭代次数少，但每一步计算量大。 | 迭代次数多，但每一步计算快。 |
| **结果** | 均收敛到最优策略。 | 均收敛到最优策略。 |

### 五、 $\epsilon$-贪心策略概率计算
**题目**：
在状态 $s$ 下有 4 个动作。$Q(s, a_1)$ 最大。
使用 $\epsilon=0.2$ 的 $\epsilon$-贪心策略。求选中 $a_1$（最优）和 $a_2$（非最优）的概率。
**解答**：
*   **非最优动作 ($a_2, a_3, a_4$)**：
    每个动作均分 $\epsilon$ 的概率。
    $P(a_2) = \frac{\epsilon}{动作总数} = \frac{0.2}{4} = \mathbf{0.05}$
*   **最优动作 ($a_1$)**：
    拿走剩下的所有概率（贪心部分 + 随机分到的部分）。
    $P(a_1) = (1 - \epsilon) + \frac{\epsilon}{4} = 0.8 + 0.05 = \mathbf{0.85}$
### 六、策略迭代及其例题
#### 一、 通用算法流程（解贝尔曼方程组）
假设有 $N$ 个状态 $s_1, s_2, \dots, s_N$。
1.  **列方程**：
    对每一个状态，写出它的贝尔曼方程：$$ V(s) = \text{即时奖励} + \gamma \times (\text{未来的平均价值}) $$$$ V(s) = R(s, \pi(s)) + \gamma \sum_{s'} P(s'|s, \pi(s)) V(s') $$
    *   你会得到 $N$ 个方程，含有 $N$ 个未知数 ($V_1, \dots, V_N$)。
2.  **整理方程（标准化）**：
    把所有含有 $V$ 的项移到等号左边，常数项（奖励）留在右边。
    形式通常是：$a V_1 + b V_2 = c$。
3.  **求解（代入消元法）**：
    *   选一个最简单的方程，把某个 $V_i$ 用其他变量表示出来（例如 $V_2 = 2 + 0.5 V_1$）。
    *   把这个式子**代入**到其他方程中，消去 $V_2$。
    *   重复直到求出第一个数值，然后回代求其他的。
#### 二、 例题
**题目描述**：
有 2 个状态：$s_1$（起点）和 $s_2$（终点附近）。
当前策略 $\pi$ 如下：
*   **在 $s_1$**：
    *   有 50% 概率原地不动（回到 $s_1$），奖励 $R=2$。
    *   有 50% 概率移动到 $s_2$，奖励 $R=4$。
*   **在 $s_2$**：
    *   **必定**回到 $s_1$（形成环路），奖励 $R=10$。
*   折扣因子 **$\gamma = 0.8$**。
**任务**：计算该策略下两个状态的真实价值 $V(s_1)$ 和 $V(s_2)$。
#### 三、 详细计算步骤
**Step 1：列出原始方程**
根据题目描述，写出两个状态的价值公式：
1.  对于 $s_1$：$$ V(s_1) = \underbrace{0.5 \times (2 + 0.8 V(s_1))}_{\text{原地不动}} + \underbrace{0.5 \times (4 + 0.8 V(s_2))}_{\text{去s2}} $$
    *(注意：奖励也是要加权的，或者理解为期望奖励 + 折扣期望未来价值)*
2.  对于 $s_2$：$$ V(s_2) = \underbrace{1.0 \times (10 + 0.8 V(s_1))}_{\text{回s1}} $$
**Step 2：化简方程**
化简方程 (1)：
$$ V(s_1) = (0.5 \times 2 + 0.5 \times 0.8 V(s_1)) + (0.5 \times 4 + 0.5 \times 0.8 V(s_2)) $$
$$ V(s_1) = 3 + 0.4 V(s_1) + 0.4 V(s_2) $$
移项整理（把 $V(s_1)$ 放到左边）：
$$ (1 - 0.4) V(s_1) = 3 + 0.4 V(s_2) $$
$$ \mathbf{0.6 V(s_1) = 3 + 0.4 V(s_2)} \quad \cdots\cdots \text{①式} $$

化简方程 (2)：
$$ V(s_2) = 10 + 0.8 V(s_1) \quad \cdots\cdots \text{②式} $$
**Step 3：代入消元求解**
现在我们有了两个线性方程：
① $0.6 V(s_1) = 3 + 0.4 V(s_2)$
② $V(s_2) = 10 + 0.8 V(s_1)$
**将 ②式 代入 ①式：**
$$ 0.6 V(s_1) = 3 + 0.4 \times [ 10 + 0.8 V(s_1) ] $$
**解这个一元一次方程：**
$$ 0.6 V(s_1) = 7 + 0.32 V(s_1) $$
**移项：**
$$ 0.28 V(s_1) = 7 $$
$$ V(s_1) = \frac{7}{0.28} = \frac{700}{28} = \mathbf{25} $$

**回代求 $V(s_2)$：**
将 $V(s_1) = 25$ 代入 ②式：
$$ V(s_2) = 10 + 20 = \mathbf{30} $$
**Step 4：最终结果**
*   $V(s_1) = 25$
*   $V(s_2) = 30$
#### 四、 考试救命技巧（防止算错）
1.  **检查符号**：
    方程永远是 $V = R + \gamma (\dots)$。
    如果你移项后发现 $V$ 的系数是负的（例如 $-0.2 V = 10$），说明你**肯定算错了**（价值通常是正的，且系数 $1-\gamma$ 必定为正）。
2.  **关于奖励 R 的写法**：
    有些教材的公式写成 $R(s,a) + \gamma V(s')$，有些写成 $P(s'|s,a)[R + \gamma V(s')]$。
    **考试万能法**：就把这一步看作：**“我这一步能拿到的平均现钱” + “打折后的下一步平均身价”**。
    *   本题 $s_1$ 的平均现钱：$0.5\times2 + 0.5\times4 = 3$。
    *   本题 $s_1$ 的下一步：$0.5$ 去 $s_1$, $0.5$ 去 $s_2$。
    *   直接列：$V_1 = 3 + 0.8(0.5 V_1 + 0.5 V_2)$。这和上面的推导结果完全一样，但更快。
3.  **如果只有两个状态**：
    直接把 $V_2$ 的表达式代入 $V_1$，一步到位，不要去列矩阵求逆，那个太慢。
# 神经网络
## 基本概念、激活函数、forward和backward的计算流程
### 一、 基础概念与数学表示
1.  **神经元模型**：**权重矩阵中，每行是一个神经元；输入矩阵中，每列是一个输入**
    *   **线性变换**：$z = Wx + b$
        *   $W$：权重矩阵（行数=当前层神经元数，列数=上一层输入数）。
        *   $b$：偏置向量。
    *   **非线性变换**：$a = \sigma(z)$
        *   $\sigma$：激活函数。
        *   $a$：输出（Activation）。
2.  **网络结构**：
    *   **输入层**：接收原始数据。
    *   **隐藏层**：提取特征，可以有多层。
    *   **输出层**：输出预测结果（分类或回归）。
3.  **计算符号约定**：
    *   $x$：输入向量（列向量）。
    *   $\odot$：**逐元素相乘**（Hadamard积）。
    *   上标 $T$：矩阵转置。
### 二、 常见激活函数及其导数
反向传播需要用到导数，必须记住：

| 激活函数 | 公式 $f(z)$ | 导数 $f'(z)$ | 特点 |
| :--- | :--- | :--- | :--- |
| **Sigmoid** | $\frac{1}{1+e^{-z}}$ | $f(z)(1-f(z))$ | 输出(0,1)，常用于二分类，导数最大0.25。 |
| **Linear** | $z$ | $1$ | 恒等映射，常用于回归问题的输出层。 |
| **ReLU** | $\max(0, z)$ | $1 (z>0), 0 (z\le0)$ | 解决梯度消失，计算快，最常用。 |
| **Tanh** | $\frac{e^z - e^{-z}}{e^z + e^{-z}}$ | $1 - (f(z))^2$ | 输出(-1,1)，零中心化。 |

- softmax 的导数很复杂，通常不会考，考也是和交叉熵损失放在一起
- 为什么要使用激活函数？为了引入非线性，否则再多层线性层的堆叠也只相当于一个巨大的线性层，拟合能力有限
### 三、 计算流程（前向与反向）
假设网络结构：输入 $x$ $\to$ 隐藏层 ($W_1, b_1, \sigma_1$) $\to$ 输出层 ($W_2, b_2, \sigma_2$) $\to$ 损失 $L$。
#### 1. 前向传播 (Forward) —— 算预测值
从左到右，一层层算：
1.  **隐藏层**：
    *   $z_1 = W_1 x + b_1$
    *   $a_1 = \sigma_1(z_1)$
2.  **输出层**：
    *   $z_2 = W_2 a_1 + b_2$
    *   $a_2 = \sigma_2(z_2)$ （即预测值 $\hat{y}$）
3.  **损失**：
    *   $L = \text{Loss}(\hat{y}, y)$ （如均方误差 $\frac{1}{2}(\hat{y}-y)^2$）
#### 2. 反向传播 (Backward) —— 算梯度
从右到左，利用**链式法则**：
1.  **输出层误差项 ($\delta_2$)**：
    $$ \delta_2 = \frac{\partial L}{\partial z_2} = \frac{\partial L}{\partial a_2} \odot \sigma_2'(z_2) $$
    *(如果是 MSE + Linear，$\delta_2 = \hat{y} - y$)*
2.  **输出层参数梯度**：
    *   $\nabla W_2 = \delta_2 \cdot a_1^T$
    *   $\nabla b_2 = \delta_2$
3.  **隐藏层误差项 ($\delta_1$)** —— **核心难点**：
    *   误差回传：$W_2^T \delta_2$ （矩阵乘法，混合误差）
    *   过激活函数：$\odot \sigma_1'(z_1)$ （逐元素乘，门控过滤）   $$ \delta_1 = (W_2^T \delta_2) \odot \sigma_1'(z_1) $$
4.  **隐藏层参数梯度**：
    *   $\nabla W_1 = \delta_1 \cdot x^T$
    *   $\nabla b_1 = \delta_1$
5.  **参数更新**（学习率 $\eta$）：
    $W \leftarrow W - \eta \nabla W$
### 四、 手算真题示例
**题目**：
*   **结构**：输入层(2维) $\to$ 隐藏层(2个神经元, Sigmoid) $\to$ 输出层(1个神经元, Linear)。
*   **输入**：$x=[1, 0]^T$，**真实值**：$y=0.8$。
*   **参数**：
    $W_1 = \begin{bmatrix} 0.1 & 0.4 \\ 0.2 & 0.3 \end{bmatrix}$ (2x2), $b_1=[0,0]^T$
    $W_2 = [0.7, 0.5]$ (1x2), $b_2=0.1$
*   **学习率**：$\eta=0.1$。
*   **任务**：计算一轮反向传播并更新 $W_1$。
**解答**：
**1. 前向传播**
*   **隐层线性**：$z_1 = W_1 x + b_1 = \begin{bmatrix} 0.1(1)+0 \\ 0.2(1)+0 \end{bmatrix} = \begin{bmatrix} 0.1 \\ 0.2 \end{bmatrix}$
*   **隐层激活** (Sigmoid)：
    $a_1 = [\sigma(0.1), \sigma(0.2)]^T \approx [0.525, 0.550]^T$
*   **输出层线性** (激活是Linear，即 $a_2=z_2$)：
    $z_2 = W_2 a_1 + b_2 = 0.7(0.525) + 0.5(0.550) + 0.1 = 0.3675 + 0.275 + 0.1 = \mathbf{0.7425}$
*   **误差**：$\hat{y} - y = 0.7425 - 0.8 = \mathbf{-0.0575}$
**2. 反向传播**
*   **输出层梯度 $\delta_2$**：
    对于 MSE + Linear，$\delta_2 = (\hat{y}-y) \times 1 = -0.0575$。
*   **隐层梯度 $\delta_1$**：
    *   **回传误差**：$W_2^T \delta_2 = \begin{bmatrix} 0.7 \\ 0.5 \end{bmatrix} (-0.0575) = \begin{bmatrix} -0.040 \\ -0.029 \end{bmatrix}$
    *   **激活导数** (Sigmoid导数 $y(1-y)$)：
        $\sigma'(z_1) = \begin{bmatrix} 0.525(1-0.525) \\ 0.550(1-0.550) \end{bmatrix} \approx \begin{bmatrix} 0.249 \\ 0.248 \end{bmatrix}$
    *   **逐元素乘**：
        $\delta_1 = \begin{bmatrix} -0.040 \\ -0.029 \end{bmatrix} \odot \begin{bmatrix} 0.249 \\ 0.248 \end{bmatrix} \approx \begin{bmatrix} -0.010 \\ -0.007 \end{bmatrix}$
*   **计算 $W_1$ 梯度**：
    $\nabla W_1 = \delta_1 \cdot x^T = \begin{bmatrix} -0.010 \\ -0.007 \end{bmatrix} [1, 0] = \begin{bmatrix} -0.010 & 0 \\ -0.007 & 0 \end{bmatrix}$
**3. 更新参数**
$W_1^{new} = W_1 - \eta \nabla W_1 = \begin{bmatrix} 0.1 & 0.4 \\ 0.2 & 0.3 \end{bmatrix} - 0.1 \begin{bmatrix} -0.010 & 0 \\ -0.007 & 0 \end{bmatrix} = \begin{bmatrix} 0.101 & 0.4 \\ 0.2007 & 0.3 \end{bmatrix}$
### 五、关于Softmax激活函数，单独说说
#### 1. 为什么 Softmax 不单独考求导？
Softmax 函数 $S_i = \frac{e^{z_i}}{\sum e^{z_j}}$ 的导数是一个 **Jacobian 矩阵**。
*   当 $i=j$ 时，导数是 $S_i(1-S_i)$。
*   当 $i \neq j$ 时，导数是 $-S_i S_j$。
*   **考点**：单独算这个太繁琐且意义不大。
#### 2. 必考组合：Softmax + Cross-Entropy
在反向传播中，这两个通常绑定在一起。因为它们组合后的梯度**非常简单优美**。
*   **损失函数**：$L = -\sum y_i \ln(S_i)$ （$y$ 是真实标签的 One-hot 编码）。
*   **神奇的结论**：
    损失函数 $L$ 对输入 $z_i$（即 Softmax 之前的线性输出）的导数是：$$ \frac{\partial L}{\partial z_i} = S_i - y_i $$
    即：**预测概率 - 真实标签**。
*   **考试应对**：
    *   如果题目问“推导 Softmax 层的梯度”，你直接写出最终结论 $\delta = \hat{y} - y$ 通常就能拿大部分分。
    *   如果非要手算，记住这个结论直接代入数值即可（例如预测 0.8，真实 1，梯度就是 -0.2）。
## MLP设计题
### 一、 MLP 设计题通用模板
如果题目让你设计一个 MLP 系统（例如分类水果、预测房价等），请按以下四个步骤作答：
#### 1. 系统总体设计（文字描述）
*   **输入模块**：说明输入是什么（如图像像素、文本向量），并提到**预处理**（归一化/标准化、Flatten 展平）。
*   **模型结构**：描述 MLP 的层级。
    *   *标准话术*：采用“输入层 + 若干隐藏层 + 输出层”的结构。隐藏层使用 **全连接 (Linear) + 批归一化 (BN) + 激活函数 (ReLU)** 的组合，以增强非线性表达能力并加速收敛。
*   **输出模块**：
    *   **分类任务**：输出层节点数 = 类别数，使用 **Softmax** 归一化为概率。
    *   **回归任务**：输出层节点数 = 1，通常无激活函数（或线性激活）。
#### 2. 关键组件选择（说明理由）
*   **损失函数**：分类用 **交叉熵 (Cross-Entropy)**；回归用 **均方误差 (MSE)**。
*   **优化器**：选用 **SGD**（随机梯度下降）或 **Adam**（自适应学习率）。
*   **防止过拟合**：提到 **Dropout**（随机失活）或 **L2 正则化**。
#### 3. 伪代码或流程图
用 PyTorch 风格的伪代码描述 `__init__`（定义层）和 `forward`（前向传播）是最高效的答题方式。
```python
class MLP(nn.Module):
    def __init__(self, in_dim, out_dim):
        self.layers = nn.Sequential(
            nn.Linear(in_dim, 128),  # 隐藏层1
            nn.BatchNorm1d(128),     # BN层
            nn.ReLU(),               # 激活函数
            nn.Dropout(0.5),         # 防过拟合
            nn.Linear(128, out_dim)  # 输出层
        )
    def forward(self, x):
        x = x.view(x.size(0), -1)    # 展平
        return self.layers(x)
```
### 三、 设计题真题示例
**题目**：设计一个 MLP 识别手写数字（MNIST，28x28像素，10类）。
**解答关键点**：
1.  **输入**：$28 \times 28 = 784$ 维向量（Flatten）。
2.  **结构**：输入层 784 $\to$ 隐藏层 256 (ReLU) $\to$ 输出层 10。
3.  **输出**：Softmax 归一化。
4.  **损失**：交叉熵损失。
5.  **代码片段**：
    ```python
    model = nn.Sequential(
        nn.Flatten(),
        nn.Linear(784, 256),
        nn.ReLU(),
        nn.Linear(256, 10) # 配合CrossEntropyLoss使用
    )
    ```

# 应用实例
为了方便记忆，我将它们按照**算法特性**进行了分类。考试时如果问到“举例说明某算法的应用”，直接套用即可。
### 一、 有监督学习：分类与回归
#### 1. 逻辑回归
*   **核心场景**：需要输出**概率**值的二分类问题，或者解释性要求较高的场景。
*   **应用实例**：
    *   **金融风控/信用评分**：预测用户违约的概率（输出0-1之间的分值）。
    *   **广告点击率预测**：预测用户点击某条广告的可能性。
    *   **医疗诊断**：根据体检指标预测患病的概率。
#### 2. 朴素贝叶斯
*   **核心场景**：特征维度高但稀疏，且假设特征独立的**文本处理**任务。
*   **应用实例**：
    *   **垃圾邮件过滤**：根据邮件中的单词频率判断是否为垃圾邮件。
    *   **新闻分类**：自动将新闻文章归类为体育、财经、科技等。
    *   **情感分析**：判断用户评论是好评还是差评。
#### 3. 决策树
*   **核心场景**：需要**清晰规则**、过程可解释的决策任务。
*   **应用实例**：
    *   **医疗辅助诊断**：医生需要根据病症一步步推导（如：发烧吗？-> 咳嗽吗？-> 确诊）。
    *   **客户流失预测**：分析哪些特征（如：合同到期、投诉次数）导致客户离开。
#### 4. 支持向量机
*   **核心场景**：**小样本**、高维特征、追求高精度的分类任务。
*   **应用实例**：
    *   **手写数字识别**：在深度学习流行前，是识别邮政编码、支票数字的主流方法。
    *   **文本分类**：处理高维文本向量。
    *   **行人检测**：利用梯度直方图特征进行二分类检测。
#### 5. K-近邻
*   **核心场景**：基于**相似度**的简单推荐或分类，数据量不大的情况。
*   **应用实例**：
    *   **简单的推荐系统**：推荐和当前用户喜好相似的其他用户喜欢的商品。
    *   **手写字符识别**：找库里最像的字。
#### 6. 神经网络（深度学习）
*   **核心场景**：处理**图像、声音、文本**等非结构化大数据，非线性极强。
*   **应用实例**：
    *   **计算机视觉**：人脸识别、自动驾驶中的物体检测、医学影像分析。
    *   **自然语言处理**：机器翻译（如谷歌翻译）、ChatGPT等生成式大模型。
    *   **语音处理**：语音转文字、语音助手。
### 二、 无监督学习：聚类与降维
#### 1. K-means 聚类
*   **核心场景**：没有标签，需要**自动分组**的任务。
*   **应用实例**：
    *   **客户细分**（用户画像）：根据消费行为将用户分为“高价值”、“价格敏感”、“沉睡用户”等群体。
    *   **图像压缩**：将图片中成千上万种颜色聚类为几十种主要颜色，减少存储空间。
    *   **异常检测**：远离聚类中心的数据点可能就是欺诈或故障。
#### 2. 主成分分析
*   **核心场景**：数据**预处理**、降维、去噪、特征提取。
*   **应用实例**：
    *   **特征脸提取**：在人脸识别中，将高维像素压缩为低维特征向量。
    *   **数据可视化**：将几十维的数据压缩到 2D 或 3D 平面上展示。
    *   **信号去噪**：丢弃特征值小的成分（视为噪声），重构信号。
### 三、 强化学习：决策与控制
#### 1. 强化学习
*   **核心场景**：智能体在环境中通过**试错**来学习连续决策，目标是长期奖励最大化。
*   **应用实例**：
    *   **游戏 AI**：AlphaGo（围棋）、王者荣耀 AI、超级马里奥自动通关。
    *   **机器人控制**：机器人学习走路、抓取物体、波士顿动力狗的平衡控制。
    *   **自动驾驶决策**：在复杂路况下决定加速、刹车或变道。
    *   **推荐系统排序**：根据用户的实时反馈调整推荐策略。
### 考试万能套路
如果考试问到一个你不熟悉的应用，可以按以下逻辑编：
1.  **分类算法**：都可以写“垃圾邮件过滤”、“肿瘤良恶性判断”。
2.  **聚类算法**：都可以写“客户群体划分”。
3.  **回归算法**：都可以写“房价预测”、“股票趋势预测”。